export enum Author {
  USER = "user",
  TOKEN = "token",
  REFRESH_TOKEN = "refreshToken",
  ROLE_ADMIN = "ROLE_ADMIN"
}

export enum TypeModal {
  CREATE = "Create",
  UPDATE = "Update",
}

export enum AlertType {
  SUCCESS = "success",
  ERROR = "error",
  LOADING = "loading",
  INFO = "info"
}

export enum AlertMessage {
  SUCCESS_CREATE = "Thêm mới thành công",
  ERROR = "Có lỗi, vui lòng thử lại",
  SUCCESS_UPDATE = "Cập nhật thành công",
}